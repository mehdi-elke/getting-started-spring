spring:
  application:
    name: getting-started-spring
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
